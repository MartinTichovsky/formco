{
  "name": "formco-repo",
  "version": "0.4.0",
  "description": "",
  "author": "Martin Tichovsky",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "yarn workspaces foreach -pti run build",
    "clean": "yarn workspaces foreach -pti run clean",
    "run:demo": "yarn workspaces foreach -ptRi --from '{demo-server,demo}' run start",
    "test": "jest --collect-coverage --colors --verbose",
    "tsc": "yarn workspaces foreach -pti run tsc"
  },
  "devDependencies": {
    "@testing-library/dom": "8.7.2",
    "@testing-library/jest-dom": "5.14.1",
    "@testing-library/react": "12.1.2",
    "@testing-library/user-event": "13.2.1",
    "jest": "27.2.5",
    "jest-collector": "1.0.2",
    "prettier": "2.4.1",
    "rimraf": "3.0.2",
    "ts-jest": "27.0.5",
    "typescript": "4.4.3"
  },
  "packageManager": "yarn@3.0.2"
}